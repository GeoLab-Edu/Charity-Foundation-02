import {withNamespaces} from "react-i18next";

function Projects({ t }) {
    return (
        <>
            <h1>Projects Works!!!</h1>
        </>
    )
}

export default withNamespaces()(Projects);
